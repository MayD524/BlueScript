let str a = "hello world"
let str b = "l"

let int i = 0
let int size = 0
sizeof a -> size

lable goto_lable
    if a[i] == b
        print a[i]
    endif
    set i = i + 1
goif i < size -> goto_lable