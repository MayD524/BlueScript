include bs_libs/stdlib


struct newType
{
    array keys = []
    array values = []
}

let newType dict
//mem 1
append "hello world" -> dict.keys
append "world" -> dict.keys
append "hello" -> dict.keys
append "he" -> dict.keys

let int test = 0
call index(dict.keys, "hello") -> test
print dict.keys[0]
